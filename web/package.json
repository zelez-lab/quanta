{
  "name": "@quanta/glue",
  "version": "0.1.0",
  "private": true,
  "description": "Quanta WebGPU FFI glue. Build-only TypeScript that compiles to quanta.js + helpers. Zero runtime npm dependencies — only TS itself, build-time only. Also hosts the Playwright headless-browser smoke harness (B‴, dev-only).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "smoke": "playwright test",
    "smoke:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.4.0"
  }
}
