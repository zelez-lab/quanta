name: Release Compiler Binary

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-14
            target: aarch64-apple-darwin
            archive: tar.gz
          - os: macos-13
            target: x86_64-apple-darwin
            archive: tar.gz
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
          - os: ubuntu-22.04-arm64
            target: aarch64-unknown-linux-gnu
            archive: tar.gz
          - os: windows-2022
            target: x86_64-pc-windows-msvc
            archive: zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust nightly
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install LLVM 22 (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          brew install llvm@22
          echo "LLVM_SYS_221_PREFIX=$(brew --prefix llvm@22)" >> $GITHUB_ENV

      - name: Install LLVM 22 (Linux)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo gpg --dearmor -o /usr/share/keyrings/llvm.gpg
          echo "deb [signed-by=/usr/share/keyrings/llvm.gpg] http://apt.llvm.org/jammy/ llvm-toolchain-jammy-22 main" | sudo tee /etc/apt/sources.list.d/llvm.list
          sudo apt-get update
          sudo apt-get install -y llvm-22-dev libpolly-22-dev
          echo "LLVM_SYS_221_PREFIX=/usr/lib/llvm-22" >> $GITHUB_ENV

      # MSYS2 is the only practical source of `llvm-sys`-compatible
      # LLVM dev packages on Windows runners. The official LLVM Windows
      # release ships clang/lld/lldb but not the static libs + headers
      # `llvm-sys` needs. Chocolatey is in the same boat.
      #
      # We install LLVM 22 via the `mingw-w64-clang-x86_64-llvm` package
      # under MSYS2's CLANG64 environment, which provides
      # `<prefix>/bin/llvm-config.exe`, the static archives, and the
      # headers `llvm-sys` introspects.
      - name: Install MSYS2 + LLVM 22 (Windows)
        if: startsWith(matrix.os, 'windows')
        uses: msys2/setup-msys2@v2
        with:
          msystem: CLANG64
          update: true
          install: >-
            mingw-w64-clang-x86_64-llvm
            mingw-w64-clang-x86_64-polly
            mingw-w64-clang-x86_64-clang
            mingw-w64-clang-x86_64-lld

      - name: Wire LLVM_SYS_221_PREFIX (Windows)
        if: startsWith(matrix.os, 'windows')
        shell: msys2 {0}
        run: |
          # MSYS2 CLANG64 root → /clang64 (Windows path: C:\msys64\clang64)
          # llvm-config lives at /clang64/bin/llvm-config.exe.
          LLVM_PREFIX="C:\msys64\clang64"
          echo "LLVM_SYS_221_PREFIX=$LLVM_PREFIX" >> $GITHUB_ENV
          # Add MSYS2 clang64 bin to PATH so DLLs are reachable at link
          # and runtime.
          echo "C:\msys64\clang64\bin" >> $GITHUB_PATH
          # Surface the version for diagnostics.
          /clang64/bin/llvm-config.exe --version

      - name: Build quanta-compiler
        run: cargo build -p quanta-compiler --release

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          cd target/release
          tar czf quanta-compiler-${{ matrix.target }}.tar.gz quanta-compiler
          # macOS doesn't ship `sha256sum`; `shasum -a 256` exists on
          # both macOS and Linux.
          if command -v sha256sum >/dev/null; then
            sha256sum quanta-compiler-${{ matrix.target }}.tar.gz \
              > quanta-compiler-${{ matrix.target }}.tar.gz.sha256
          else
            shasum -a 256 quanta-compiler-${{ matrix.target }}.tar.gz \
              > quanta-compiler-${{ matrix.target }}.tar.gz.sha256
          fi

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          cd target/release
          Compress-Archive -Path quanta-compiler.exe -DestinationPath quanta-compiler-${{ matrix.target }}.zip
          $hash = (Get-FileHash quanta-compiler-${{ matrix.target }}.zip -Algorithm SHA256).Hash.ToLower()
          "$hash  quanta-compiler-${{ matrix.target }}.zip" | Out-File -Encoding ASCII quanta-compiler-${{ matrix.target }}.zip.sha256

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: quanta-compiler-${{ matrix.target }}
          path: |
            target/release/quanta-compiler-${{ matrix.target }}.tar.gz
            target/release/quanta-compiler-${{ matrix.target }}.tar.gz.sha256
            target/release/quanta-compiler-${{ matrix.target }}.zip
            target/release/quanta-compiler-${{ matrix.target }}.zip.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            quanta-compiler-*.tar.gz
            quanta-compiler-*.zip
            quanta-compiler-*.sha256
          generate_release_notes: true
