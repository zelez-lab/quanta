[package]
name = "quanta-ir"
version = "0.1.0"
edition = "2024"
description = "Quanta kernel intermediate representation — shared between proc macro and compiler"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1"
